export const ACCELERATION_STEPS = [5, 7, 9, 7, 5, 4, 3, 2, 1]

export const INITIAL_BOARD_POSITION = Object.freeze({
  top: 0,
  left: 0
})

export const INTIAL_PIECE_POSITION = Object.freeze({
  top: 10,
  left: 10
})

export const STEP_SIZE = 5
export const GAME_BOARD_SIZE = 300
export const GAME_BOARD_BORDER_SIZE = 5

export const PLAYER_SIZE = 10

export const THING_SIZE = 20
export const THING_INITIAL_POSITION = Object.freeze({
  top: 40,
  left: 50
})

const ARROW_UP = 'ArrowUp'
const ARROW_RIGHT = 'ArrowRight'
const ARROW_DOWN = 'ArrowDown'
const ARROW_LEFT = 'ArrowLeft'
const UP = 'UP'
const RIGHT = 'RIGHT'
const DOWN = 'DOWN'
const LEFT = 'LEFT'
const TOP = 'TOP'
const BOTTOM = 'BOTTOM'
export const STOP = 'STOP'

export const DIRECTIONS = Object.freeze({
  UP,
  RIGHT,
  DOWN,
  LEFT
})

export const INPUT_DIRECTIONS = Object.freeze({
  [ARROW_UP]: 'UP',
  [ARROW_RIGHT]: 'RIGHT',
  [ARROW_DOWN]: 'DOWN',
  [ARROW_LEFT]: 'LEFT'
})
export const SIDES = Object.freeze({
  TOP,
  RIGHT,
  BOTTOM,
  LEFT
})